import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asadbek.ishritmi',
  appName: 'Ish Ritmi',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
